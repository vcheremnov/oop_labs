#pragma once

// game states
enum class GameState {
    SplashScreen,
    MenuSelect,
    ShipPlacement,
    Battle,
    Stats,
    QuitScreen
};

// player's number
enum class PlayerNumber {
    Player1, Player2
};
